export interface IPerson {
  getRandomName:(el, arr) => string
  getRandomBalance: (el) => number
  getRandomAge: (el) => number
  getRandomDocument: (el) => number
  getRandomEnglishLevel: (el, arr) => string
}
