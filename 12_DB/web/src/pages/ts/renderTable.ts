export function renderTable(person){

  // const tr = document.createElement("tr");
  // tr.setAttribute("data-rowid", person.ID);
  //
  // const idTd = document.createElement("td");
  // idTd.append(person.ID);
  // tr.append(idTd);
  //
  // const nameTd = document.createElement("td");
  // nameTd.append(person.FirstName);
  // tr.append(nameTd);
  //
  // const lastTd = document.createElement("td");
  // nameTd.append(person.LastName);
  // tr.append(lastTd);
  //
  // const ageTd = document.createElement("td");
  // ageTd.append(person.Age);
  // tr.append(ageTd);
  //
  // const streetTd = document.createElement("td");
  // ageTd.append(person.Id_Street);
  // tr.append(streetTd);
  //
  // return tr;
}
