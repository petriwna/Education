
describe('add', () => {
    test('add element to list', () => {

    });
});

describe('clear', () => {
    test('clear list', () => {

    });
});

describe('getSize', () => {
    test('get size list', () => {

    });
});

describe('set', () => {
    test('set element to index', () => {

    });
});

describe('get', () => {
    test('get element at index', () => {

    });
});

describe('remove', () => {
    test('remove element add return him', () => {

    });
});

describe('toArray', () => {
    test('return list', () => {

    });
});

describe('toString', () => {
    test('return string', () => {

    });
});

describe('contains', () => {
    test('is element in list', () => {

    });
});

describe('minValue', () => {
    test('return min element list', () => {

    });
});

describe('maxValue', () => {
    test('return max element list', () => {

    });
});

describe('minIndex', () => {
    test('return index min element list', () => {

    });
});

describe('maxIndex', () => {
    test('return index max element list', () => {

    });
});

describe('reverse', () => {
    test('reverse elements list', () => {

    });
});

describe('halfReverse', () => {
    test('reverse half list', () => {

    });
});

describe('retainAll', () => {
    test('retain coincidence in list', () => {

    });
});

describe('removeAll', () => {
    test('remove coincidence in list', () => {

    });
});

describe('sort', () => {
    test('sort list', () => {

    });
});

describe('print', () => {
    test('print list to console', () => {

    });
});